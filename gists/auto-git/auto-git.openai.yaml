interface:
  display_name: "Auto Git"
  short_description: "Commit, push, merge, and worktree safely"
  default_prompt: "Use $auto-git to inspect Git topology, group dirty changes by change intent, and run the requested checkpoint, sync, land, or worktree flow."
